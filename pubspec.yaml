name: uri_parser
description: A minimal utility to parse URIs.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  path: ^1.8.0
  safe_local_storage:
    git:
      url: https://github.com/harmonoid/safe_local_storage.git
      ref: master

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
